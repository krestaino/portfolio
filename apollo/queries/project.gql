query ($slug: String!) {
  projects(where: {
    slug: $slug
  }) {
    title
    type
    github
    url
    description
    tags {
      title
      slug
    }
    screenshots {
      name
      url
    }
  }
}
